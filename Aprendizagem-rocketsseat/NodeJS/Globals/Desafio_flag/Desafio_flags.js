/*
    Crie 2 Arquivos JavaScript
    
    O primeiro, irá exportar uma função chamada getFlag()
    que recebera um argument odo tipo String
    Essa função devera buscar dentro do array process.argv a flag desejada, que é  a string e retornar o valor  da flag

    O segundo ira importa a função e passar a flag desejada 

    Iremos rodar no terminal o segundo arquivo passando as flags
    --name e --greetings para que seja impresso no terminal a 
    saudação e oo nome  da pessoa
*/