// Switch (varios caminhos, semelhante ao if)\

let result
switch(expression){
    case 'a': // começo do caso
        //codigo aqui
        break;// sinaliza o final do caso
    case 'b':
        //codigo aqui   
        break;
    default: // não cumpriu nenhum dos casos acima? 
    break;//entao saia
}